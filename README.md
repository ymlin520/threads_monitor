# Threads 監測系統（threads_monitor）

在自己電腦上跑的 Threads（脆）輿情與帳號分析系統。系統每天自動開 Chrome 讀取 Threads 的公開頁面，把指定關鍵字、主題標籤與帳號的貼文、互動數、瀏覽數、留言與粉絲數存進本機資料庫，再用網頁儀表板呈現分析結果。

- **前台（分析）**：總覽、主題監測、主題交叉比較、貼文成效、瀏覽分析、帳號比較、最佳發文時段、內容成功模式
- **後台（管理）**：監測設定、抓取與排程、成員、系統設定
- **團隊**：多個工作區、帳號密碼登入、成員角色（管理員／編輯／檢視者）
- **報表**：一鍵匯出多工作表 Excel，貼文列表可另存 CSV

不需要 Threads 官方 API、不需要 Threads 帳號、不需要任何雲端服務，所有資料都在本機的一個 SQLite 檔案裡。

---

## 目錄

1. [系統需求](#系統需求)
2. [安裝與啟動](#安裝與啟動)
3. [第一次使用](#第一次使用)
4. [功能說明（前台・分析）](#功能說明前台分析)
5. [功能說明（後台・管理）](#功能說明後台管理)
6. [團隊、工作區與權限](#團隊工作區與權限)
7. [抓取流程與收錄規則](#抓取流程與收錄規則)
8. [指標與演算法定義](#指標與演算法定義)
9. [報表匯出](#報表匯出)
10. [資料來源的限制](#資料來源的限制)
11. [使用登入狀態提高抓取量（選用）](#使用登入狀態提高抓取量選用)
12. [讓同網路的同事連線](#讓同網路的同事連線)
13. [專案結構](#專案結構)
14. [資料庫結構](#資料庫結構)
15. [API 一覽](#api-一覽)
16. [環境變數](#環境變數)
17. [備份、搬移與重設](#備份搬移與重設)
18. [疑難排解](#疑難排解)
19. [未包含的功能](#未包含的功能)
20. [安全性說明](#安全性說明)

---

## 系統需求

| 項目 | 需求 |
|---|---|
| 作業系統 | Windows 10/11、macOS、Linux（開發與實測在 Windows 11） |
| Node.js | **22.13 以上**（使用 Node 內建的 `node:sqlite`，不需另外安裝資料庫） |
| 瀏覽器 | 系統已安裝 **Google Chrome** 或 **Microsoft Edge**（爬蟲直接用系統瀏覽器，不另外下載 Chromium） |
| 網路 | 可以連到 `https://www.threads.com` |

確認 Node 版本：

```bash
node -v
```

---

## 安裝與啟動

```bash
git clone https://github.com/ymlin520/threads_monitor.git
cd threads_monitor
npm install
npm start
```

啟動後終端機會顯示：

```
Threads 監測系統已啟動：http://127.0.0.1:3900
```

用瀏覽器打開 http://127.0.0.1:3900 。

| 指令 | 用途 |
|---|---|
| `npm start` | 啟動網頁伺服器（含每日排程） |
| `npm run crawl` | 不開網頁，直接在終端機抓一次（適合搭配 Windows 工作排程器或 cron） |

> 伺服器要一直開著，排程才會執行。電腦關機或伺服器關閉時，那一次排程會直接略過，不會補跑。

---

## 第一次使用

1. **建立系統擁有者**：第一次打開網頁會出現「建立管理員帳號」，輸入帳號、顯示名稱、密碼（至少 8 字元）與第一個工作區名稱。這個帳號是系統擁有者，可以管理所有工作區與系統設定。系統會自動在第一個工作區放一個預設主題「世新」（可以刪除）。
2. **新增監測項目**：到「後台・管理 → 監測設定」
   - 主題：選「關鍵字」或「主題標籤」，輸入字詞與天數（預設 3 天）
   - 帳號：輸入 `@帳號` 或貼上個人頁網址，選「自己的帳號」或「競品」
3. **抓第一次**：到「抓取與排程」按「立即抓取」。會跳出一個 Chrome 視窗自動搜尋與捲動，約 3–15 分鐘（依主題與帳號數量），跑完視窗自動關閉。頁面上可以看即時紀錄。
4. **看分析**：回到「前台・分析」各頁。右上角可切換期間（近 7／14／30／90 天）與匯出 Excel。
5. **設定排程**：系統擁有者到「系統設定」調整每日抓取時間（預設 10:30，可設多個，例如 `10:30, 18:00`）。

---

## 功能說明（前台・分析）

所有分析頁都以「目前選擇的工作區」與右上角的「期間」為範圍。工作區看得到的貼文 = 被該工作區主題抓到的貼文 ∪ 該工作區監測帳號發的貼文。

### 總覽
- 指標卡：貼文數、總互動、平均／中位互動、總瀏覽、已抓留言數（正／中／負）、負面標記數
- 每日新貼文（依發布日，台灣時間）
- 每日留言情緒（正面／中性／負面堆疊）
- 互動最高的 5 篇貼文
- 需要注意的留言：含負面關鍵字或判斷為負面，依按讚數排序
- 負面關鍵字出現次數排行
- 最近一次抓取摘要

### 主題監測
- **主題熱度排名**：每個主題的提及數、總互動、平均互動、互動中位數、平均瀏覽、提及趨勢、互動趨勢、負面標記數。點欄位標題可切換排名依據。
- **熱度趨勢**：選定主題的每日提及數與每日互動數（兩張圖、各自一個刻度，不做雙軸）。
- **主題熱門貼文**：選定主題中總互動最高的 10 篇，適合研究爆文結構。

### 主題交叉比較
- 用色塊選擇要比較的主題（最多 8 個）
- 每日提及數、每日互動數折線圖
- **互動分布**：每個主題的貼文落在「0–9／10–99／100–999／1,000 以上」哪個互動級距，分辨「少數爆文」還是「普遍有人互動」
- 數字對照表：提及數、討論佔比、總互動、平均、中位數、平均瀏覽、趨勢

### 貼文成效
- **全部串文成效列表**：發布時間、作者、內文、讚、留言、轉發、分享、總互動、較上次、瀏覽、互動率、留言情緒、負面標記。每欄可排序，點列開啟詳情。
- **來源篩選**：工作區全部、自己的帳號、競品帳號、單一主題、單一帳號；可搜尋內文。
- **貼文互動比較**：勾選 2–5 篇，並排比較讚、留言、轉發、分享。
- 下載 CSV。

### 貼文詳情
- 完整內文、作者、發布時間、所屬主題、情緒與負面標記、Threads 原文連結
- 讚、留言、轉發、分享、瀏覽、每千次瀏覽互動數、互動率
- 互動成長與瀏覽成長曲線（需開啟「重新追蹤已抓過的貼文」且至少抓兩次）
- 留言列表：作者、時間、讚數、「原作者說讚」、情緒、負面關鍵字；可篩選「負面關鍵字／負面／正面／中性」

### 瀏覽分析
- 有瀏覽資料的貼文數、瀏覽中位數、每千次瀏覽互動數（中位）
- **瀏覽 × 互動散佈圖**（橫軸對數刻度）：右下角＝看的人多但不太互動
- 每篇貼文的曝光判斷：曝光與互動俱佳／高曝光低互動／曝光偏低／一般（定義見[指標與演算法定義](#指標與演算法定義)）

### 帳號比較
- **帳號總覽**：粉絲數、期間粉絲成長、成長率、每日成長、貼文數、每週發文、平均互動、互動中位數、互動率、平均瀏覽、最佳貼文
- **粉絲成長速度**：各帳號粉絲數量級不同，畫「相對第一筆紀錄的成長 %」，所有帳號共用一個刻度才能比較動能
- **一對一比較**：選兩個帳號，逐項比較並標出哪一方較高；附每週平均互動與每週發文數圖

### 最佳發文時段
- 星期 × 3 小時時段的熱力圖，格內顯示平均互動與篇數，顏色越深平均互動越高
- 建議發文時段：只列出至少 2 篇貼文的時段，依互動**中位數**排序（避免單篇爆文把整格拉高）
- 預設來源是「自己的帳號」（有設定的話）；期間至少取近 30 天

### 內容成功模式
- 把貼文依總互動排序，前 25% 當「高成效」，與其他貼文比較 11 項內容特徵：附圖片或影片、附影片、含問句、使用表情符號、有主題標籤、標記其他帳號、附連結、開頭第一行 15 字內、開頭用數字、含驚嘆號、長文（120 字以上），以及平均字數
- 自動產生差距 15 個百分點以上的洞察句
- 高成效貼文常見詞：在高成效貼文中出現比例明顯較高的雙字詞，可當題材靈感
- 高成效範例貼文
- 至少需要 8 篇有內文的貼文才會分析

---

## 功能說明（後台・管理）

### 監測設定（編輯以上）
- **主題**：類型（關鍵字／主題標籤）、字詞、天數（1–30）、啟用開關、刪除
  - 關鍵字：Threads 搜尋結果中，內文或主題標籤含該字詞的貼文（不分大小寫）
  - 主題標籤：直接採用 Threads 標籤搜尋（`serp_type=tags`）的結果，輸入時 `#` 可有可無
- **帳號**：帳號、顯示名稱、類型（自己／競品）、目前粉絲數、最後更新時間、啟用開關、刪除
- 刪除主題或帳號不會刪除已抓到的貼文

### 抓取與排程
- 「立即抓取」（編輯以上）：所有工作區的主題與帳號會一起抓，同一個字詞或帳號只抓一次
- 即時紀錄：抓取中每 2 秒更新
- 抓取紀錄：每次的開始時間、觸發者、耗時、狀態、主題收錄數、新貼文、略過重複、帳號數、點進篇數、留言數，可展開完整紀錄

### 成員（管理員）
- 修改工作區名稱
- 新增成員：輸入已存在的帳號會直接加入；新帳號需設定初始密碼
- 調整角色、移出成員

### 系統設定（系統擁有者）
| 設定 | 預設 | 說明 |
|---|---|---|
| 啟用每日排程 | 開 | |
| 排程時間 | `10:30` | 多個用逗號分隔，使用這台電腦的本地時間 |
| 追蹤近幾天的貼文 | 7 | 只有這段期間內的貼文會被點進去抓留言與瀏覽數 |
| 每次最多點進幾篇 | 20 | 點進越多篇越久，也越容易被 Threads 限制 |
| 個人頁捲動次數 | 4 | 帳號個人頁往下捲幾次 |
| 重新追蹤已抓過的貼文 | 關 | 見[收錄規則](#抓取流程與收錄規則) |
| 瀏覽器 | Chrome | 沒有 Chrome 的電腦改選 Edge |
| 背景執行 | 關 | 不開視窗執行，較容易被 Threads 擋 |
| 分析字典 | 內建 | 負面輿情關鍵字、正面詞、負面詞；改完按「重新分析全部」套用到舊資料 |

另外可以建立／刪除工作區、查看所有使用者、重設他人密碼、刪除使用者。

### 我的帳號
左下角「我的帳號」可以修改自己的密碼。

---

## 團隊、工作區與權限

- **工作區**：每個工作區有自己的主題、監測帳號與成員，互不干擾。適合「不同部門」「不同客戶」各自一個工作區。
- **貼文資料是共用的**：同一篇貼文如果同時被兩個工作區的主題抓到，只會存一份，但各工作區只看得到自己範圍內的貼文。

| 角色 | 看分析 | 管理主題與帳號、立即抓取 | 管理成員、改工作區名稱 | 系統設定、工作區、所有使用者 |
|---|:-:|:-:|:-:|:-:|
| 檢視者 viewer | ✓ | | | |
| 編輯 editor | ✓ | ✓ | | |
| 管理員 admin | ✓ | ✓ | ✓ | |
| 系統擁有者 | ✓ | ✓ | ✓ | ✓（對所有工作區都是管理員） |

- 密碼以 scrypt 加鹽雜湊儲存
- 登入 session 有效 14 天，存在 HttpOnly cookie
- 同一帳號連續輸錯 5 次密碼，鎖定 5 分鐘

---

## 抓取流程與收錄規則

每次抓取依序做這些事：

1. 開 Chrome，先進 Threads 首頁暖身
2. **主題搜尋**：每個啟用中的主題進 Threads 搜尋頁，捲動收集貼文，直到遇到登入牆
3. **帳號個人頁**：每個啟用中的帳號進個人頁，記錄粉絲數，收集可見貼文（轉發別人的貼文不算）
4. **逐篇點進貼文頁**：取瀏覽數、最新互動數與留言（排除頁面下方的「相關串文」）
5. 關閉瀏覽器，寫入抓取紀錄

### 收錄規則（預設）

- **已經抓過的貼文不重抓，也不再點進去。** 同一篇如果也符合別的主題，只在資料庫補上主題標記，不會重新連線。
- **主題搜尋的新貼文：今天有就只收今天的；今天沒有，才收近 N 天的**（N＝該主題設定的「天數」，預設 3）。「今天」以台灣時間計算。
  - 例：9/14 搜尋到 3 篇 9/14 的新貼文、2 篇 9/13 的新貼文 → 只收 9/14 的 3 篇
  - 例：9/15 搜尋不到 9/15 的新貼文、但有 9/13、9/14 各 1 篇還沒收過 → 收這 2 篇
- **帳號的粉絲數每次都會記錄**（粉絲數不是貼文，不受上面規則影響），才看得出成長速度。
- 抓取紀錄會顯示每個主題「符合幾篇、已抓過略過幾篇、最後收了哪幾篇」。

### 重新追蹤已抓過的貼文（選用）

在「系統設定」打開後：每次抓取會回頭更新已抓過貼文的讚數、留言與瀏覽數（近「追蹤天數」內的貼文），這樣「貼文詳情」的互動成長曲線、貼文列表的「較上次」欄才會有資料。代價是每次抓取時間變長、也比較容易被 Threads 限制。

---

## 指標與演算法定義

| 指標 | 定義 |
|---|---|
| 總互動 | 讚 ＋ 留言 ＋ 轉發 ＋ 分享 |
| 平均互動 | 總互動 ÷ 貼文數 |
| 互動中位數 | 所有貼文互動數的中位數；與平均一起看，避免被少數爆文誤導 |
| 互動率 | 貼文總互動 ÷ 作者粉絲數 × 100%（只有監測清單中的帳號有粉絲數）；帳號互動率＝平均互動 ÷ 粉絲數 |
| 每千次瀏覽互動 | 總互動 ÷ 瀏覽數 × 1000 |
| 提及數 | 期間內被該主題收錄的貼文數 |
| 討論佔比 | 該主題提及數 ÷ 所有選取主題的提及數 |
| 較上次 | 最新一筆互動快照 − 前一筆快照 |
| 期間粉絲成長 | 期間內最後一筆粉絲快照 − 第一筆（少於兩筆顯示「—」） |
| 每日成長 | 期間粉絲成長 ÷ 兩筆快照相隔天數 |
| 每週發文 | 期間貼文數 ÷（期間天數 ÷ 7） |

**熱度趨勢**：把「開始監測以來」的期間切成前後兩半，比較後半與前半的提及數（或互動數）：
- 變化 ≥ +20% → 上升；≤ −20% → 下降；其他 → 持平
- 前半為 0、後半有資料 → 上升（新出現）
- 開始監測後可比較的時間不足 4 天 → 資料累積中

**曝光判斷**（以期間內有瀏覽資料的貼文為母體）：
- 瀏覽 < 瀏覽中位數 × 0.5 → 曝光偏低
- 每千次瀏覽互動 < 其中位數 × 0.5 → 高曝光低互動
- 瀏覽 ≥ 中位數 且 每千次瀏覽互動 ≥ 中位數 → 曝光與互動俱佳
- 其他 → 一般

**情緒判斷**（字典式，可在系統設定調整）：
- 每出現一個正面詞 +1；正面詞前一個字是「不、沒、別、無、非」時改為 −1（例如「不喜歡」）
- 出現負面詞或負面輿情關鍵字，每個不同的詞 −1
- 總分 > 0 正面、< 0 負面、= 0 中性
- 出現任何負面輿情關鍵字（預設：性騷、性侵、偷拍、霸凌、歧視、爆料、申訴、抗議、停招、退場、詐騙、漏水、竊盜、超收、提告、地檢署、黑箱、欠薪、資遣、裁員、性平）就標 ⚠

**最佳發文時段**：依發文時間（台灣）把貼文分到「星期 × 3 小時」共 56 格，計算每格平均與中位互動；建議時段取篇數 ≥ 2 的格子中中位數最高的前三名。

**內容成功模式**：依總互動排序，前 25%（至少 2 篇）為高成效組，其餘為對照組，比較各特徵在兩組的出現比例；差距 ≥ 15 個百分點產生洞察句。常見詞以雙字漢字詞計算「在幾成貼文中出現」，保留在高成效組出現 ≥ 2 次且比例高於對照組者。

---

## 報表匯出

- **Excel 報表**（右上角「匯出 Excel 報表」）：依目前工作區與期間產生，包含 5 張工作表
  1. 總覽：各項指標
  2. 主題熱度：各主題的提及、互動、平均、中位數、瀏覽、趨勢、最熱門貼文
  3. 貼文成效：所有貼文的完整數字、情緒、負面關鍵字、連結（依總互動排序）
  4. 留言：所有已抓留言，含負面關鍵字的排最前面
  5. 帳號比較：各帳號粉絲、成長、發文頻率、互動率
- **CSV**（貼文成效頁）：依目前篩選條件下載，UTF-8 含 BOM，Excel 直接開不會亂碼

---

## 資料來源的限制

這套系統讀的是 Threads 對**未登入訪客**公開顯示的內容，實測限制如下：

| 項目 | 訪客模式實測 |
|---|---|
| 每個主題搜尋 | 約 5–12 篇就遇到登入牆 |
| 每個帳號個人頁 | 約 10 篇 |
| 每篇貼文留言 | 約 25 則 |
| 搜尋排序 | 不照時間排序（沒有「最新」分頁），會混入較舊的貼文 |
| 瀏覽數 | 只能在貼文頁讀到，而且部分貼文（多半是互動極少的）不顯示「次瀏覽」 |

因此所有統計都是「可見範圍內」的數字，不是完整母體，頁面上也會標示。每天持續抓取，資料會逐日累積，趨勢與最佳時段通常要跑一兩週才有參考價值。

其他：
- Threads 若改版，爬蟲的 DOM 判讀可能失效（抓不到貼文或數字），需要調整 `server/crawler.js`
- 頻繁抓取可能被 Threads 暫時限制，建議每天 1–2 次
- 情緒判斷是字典比對，無法理解反諷與語境（例如「學費都拿來蓋 KTV」會判為中性）

---

## 使用登入狀態提高抓取量（選用）

如果把 Playwright 的登入狀態檔放在 `data/auth_state.json`，爬蟲會帶著登入狀態抓，搜尋會切到「最新」分頁、也不會遇到登入牆，數量會多很多。

產生方式（**登入動作由你自己在瀏覽器中完成，系統不保存帳號密碼**）：

```bash
npx playwright codegen --channel=chrome --save-storage=data/auth_state.json https://www.threads.com/login
```

在跳出的瀏覽器中登入 Threads，登入完成後關閉視窗，檔案就會存好。下一次抓取的紀錄會顯示「使用 data/auth_state.json 的登入狀態」。

注意：
- `auth_state.json` 等同你的登入 cookie，**不要分享、不要上傳**（`data/` 已在 `.gitignore`）
- 建議使用專門用來監測的帳號
- 登入狀態過期後重新產生即可
- 使用登入狀態自動化瀏覽可能違反 Threads 使用條款，請自行評估

---

## 讓同網路的同事連線

預設只聽 `127.0.0.1`（只有這台電腦能開）。要讓同網路的人連線：

Windows PowerShell：
```powershell
$env:HOST="0.0.0.0"; npm start
```

macOS／Linux：
```bash
HOST=0.0.0.0 npm start
```

同事用 `http://<這台電腦的 IP>:3900` 開啟，由管理員在「成員」頁幫他們建立帳號。可能需要在防火牆允許 3900 埠。系統沒有 HTTPS，請只在信任的內部網路使用。

---

## 專案結構

```
threads_monitor/
├── package.json
├── README.md
├── server/
│   ├── index.js        # Express 伺服器：所有 API 路由、權限、靜態檔案
│   ├── db.js           # SQLite 連線、資料表、欄位遷移、系統設定
│   ├── auth.js         # 密碼雜湊、session、登入防暴力、工作區權限 middleware
│   ├── crawler.js      # Playwright 爬蟲：主題搜尋、個人頁、貼文頁、收錄規則
│   ├── analyze.js      # 字典式情緒判斷與負面關鍵字、重新分析全部
│   ├── analytics.js    # 所有分析查詢：總覽、主題、瀏覽、帳號、時段、成功模式
│   ├── report.js       # Excel 報表與 CSV 匯出
│   ├── scheduler.js    # 每日排程
│   └── crawl-cli.js    # npm run crawl 用的命令列入口
├── public/
│   ├── index.html      # 單頁應用外框
│   ├── app.css         # 樣式（支援深色模式、手機版）
│   └── app.js          # 前端：hash 路由、各頁畫面、圖表（Chart.js）
└── data/               # 執行後自動建立（不進版控）
    ├── threads.db      # 所有資料
    └── auth_state.json # 選用：Threads 登入狀態
```

技術：Node.js（ES modules）、Express 4、`node:sqlite`、Playwright、Chart.js 4、ExcelJS。前端不需要打包工具。

---

## 資料庫結構

檔案：`data/threads.db`（SQLite，WAL 模式）

| 資料表 | 內容 |
|---|---|
| `users` | 使用者（帳號、顯示名稱、密碼雜湊、是否為系統擁有者） |
| `workspaces` | 工作區 |
| `memberships` | 使用者 × 工作區 × 角色 |
| `sessions` | 登入 session |
| `topics` | 監測主題（工作區、類型、字詞、天數、啟用） |
| `accounts` | 監測帳號（工作區、handle、名稱、自己／競品、啟用） |
| `profiles` | 帳號最新資料（名稱、簡介、粉絲數） |
| `profile_snapshots` | 每次抓取的粉絲數快照 |
| `posts` | 貼文（作者、主題標籤、內文、媒體類型、發布時間、最新互動數、瀏覽、情緒、負面關鍵字、首次／最後看到、點進時間） |
| `post_topics` | 貼文 × 主題 |
| `post_metrics` | 每次抓取的貼文互動快照 |
| `comments` | 留言（作者、內容、時間、讚數、原作者說讚、情緒、負面關鍵字） |
| `runs` | 抓取紀錄（統計數字與完整紀錄文字） |
| `settings` | 系統設定（key／value） |

舊版資料庫在啟動時會自動補上新增的欄位，不需要手動遷移。

---

## API 一覽

所有 API 回傳 JSON。除首次設定與登入外都需要登入（cookie）。`/api/ws/*` 需要以 `X-Workspace` 標頭或 `?ws=` 參數指定工作區。

**登入**
| 方法 | 路徑 | 說明 |
|---|---|---|
| GET | `/api/setup-status` | 是否需要首次設定 |
| POST | `/api/setup` | 建立系統擁有者與第一個工作區 |
| POST | `/api/login` / `/api/logout` | 登入／登出 |
| GET | `/api/me` | 目前使用者與可用工作區 |
| POST | `/api/me/password` | 修改自己的密碼 |

**分析（檢視者以上）**：常用參數 `days`、`topic`、`handle`、`kind`（own／competitor）、`sort`、`q`、`limit`
| 方法 | 路徑 | 說明 |
|---|---|---|
| GET | `/api/ws/overview` | 總覽 |
| GET | `/api/ws/posts` | 貼文列表（`sort`：newest、inter、likes、replies、views、er、negative） |
| GET | `/api/ws/posts/:code` | 貼文詳情、互動快照、留言 |
| GET | `/api/ws/topics/summary` | 主題熱度摘要 |
| GET | `/api/ws/topics/daily` | 主題每日提及與互動 |
| GET | `/api/ws/views` | 瀏覽分析 |
| GET | `/api/ws/accounts/summary` | 帳號總覽 |
| GET | `/api/ws/accounts/followers` | 粉絲成長序列 |
| GET | `/api/ws/accounts/compare?a=&b=` | 一對一比較 |
| GET | `/api/ws/best-times` | 最佳發文時段 |
| GET | `/api/ws/patterns` | 內容成功模式 |
| GET | `/api/ws/export.xlsx` | Excel 報表 |
| GET | `/api/ws/export/posts.csv` | 貼文 CSV |
| GET | `/api/ws/runs`、`/api/ws/runs/:id/log` | 抓取紀錄 |
| GET | `/api/crawl/status` | 目前抓取狀態與下一次排程 |

**管理（編輯以上）**
| 方法 | 路徑 | 說明 |
|---|---|---|
| GET／POST | `/api/ws/topics` | 列出／新增主題 |
| PATCH／DELETE | `/api/ws/topics/:id` | 修改（天數、啟用）／刪除 |
| GET／POST | `/api/ws/accounts` | 列出／新增帳號 |
| PATCH／DELETE | `/api/ws/accounts/:id` | 修改（類型、啟用、名稱）／刪除 |
| POST | `/api/ws/crawl` | 立即抓取 |

**成員（管理員）**
| 方法 | 路徑 | 說明 |
|---|---|---|
| GET／POST | `/api/ws/members` | 列出／新增成員 |
| PATCH／DELETE | `/api/ws/members/:uid` | 改角色／移出 |
| PATCH | `/api/ws/` | 改工作區名稱 |

**系統（系統擁有者）**
| 方法 | 路徑 | 說明 |
|---|---|---|
| GET／PUT | `/api/system/settings` | 系統設定 |
| POST | `/api/system/reanalyze` | 用目前字典重新分析全部貼文與留言 |
| GET／POST | `/api/system/workspaces` | 列出／建立工作區 |
| DELETE | `/api/system/workspaces/:id` | 刪除工作區 |
| GET | `/api/system/users` | 所有使用者 |
| POST | `/api/system/users/:id/password` | 重設密碼 |
| DELETE | `/api/system/users/:id` | 刪除使用者 |

---

## 環境變數

| 變數 | 預設 | 說明 |
|---|---|---|
| `HOST` | `127.0.0.1` | 監聽位址；`0.0.0.0` 開放同網路連線 |
| `PORT` | `3900` | 監聽埠 |
| `TM_DATA_DIR` | `./data` | 資料夾位置（資料庫與登入狀態檔）；測試時可指向另一個資料夾，避免動到正式資料 |

也可以用 Node 的 `--env-file`：

```bash
node --env-file=.env --no-warnings server/index.js
```

---

## 備份、搬移與重設

- **備份**：停止伺服器後複製 `data/threads.db`（若有 `threads.db-wal`、`threads.db-shm` 一併複製）
- **搬到新電腦**：在新電腦 `git clone`、`npm install`，再把 `data/` 整個資料夾複製過去
- **忘記系統擁有者密碼**：若還有其他系統擁有者可從「系統設定 → 所有使用者」重設；否則需用 SQLite 工具修改 `users` 表
- **全部重來**：停止伺服器、刪除 `data/threads.db*`，重新啟動會回到首次設定

---

## 疑難排解

| 狀況 | 原因與處理 |
|---|---|
| `Error: No such built-in module: node:sqlite` | Node 版本太舊，請升級到 22.13 以上 |
| 抓取失敗：`browserType.launch: Chromium distribution 'chrome' is not found` | 沒有安裝 Chrome，到系統設定把瀏覽器改成 Edge |
| 抓取紀錄顯示「一篇都沒掃到」「被導向登入頁」 | 被 Threads 限制或 Threads 改版；隔一段時間再試、降低抓取頻率，或改用登入狀態 |
| 「讀不到粉絲數」 | 帳號不存在、是私人帳號，或輸入錯誤 |
| 瀏覽分析沒有資料 | 貼文瀏覽量太低 Threads 不公開顯示，或還沒被點進去（每次點進有上限） |
| 互動成長曲線一直是「至少要抓兩次」 | 預設不重抓舊貼文；到系統設定打開「重新追蹤已抓過的貼文」 |
| 趨勢一直顯示「資料累積中」 | 開始監測不到 4 天，持續抓取即可 |
| 排程沒有執行 | 伺服器要開著；確認系統設定已啟用排程、時間格式為 `HH:MM` |
| 啟動時出現 `ExperimentalWarning: SQLite` | `npm start` 已加 `--no-warnings`；直接用 node 執行時的提示可忽略 |
| 抓取紀錄停在「中斷」 | 抓取途中伺服器被關閉，重新抓一次即可 |

---

## 未包含的功能

以下功能需要 Threads 官方 API 並授權帳號，本系統**沒有**實作：

- 排程發文（自動發布貼文）
- 粉絲輪廓（追蹤者年齡、性別、地區）
- 多帳號綁定（以 OAuth 授權管理多個 Threads 帳號）
- 私人帳號、限時內容的資料

---

## 安全性說明

- 預設只允許本機連線；開放區網時沒有 HTTPS，請只在信任的網路使用
- 密碼以 scrypt 加鹽雜湊儲存，session 為 HttpOnly、SameSite=Lax cookie
- `data/`（資料庫與登入狀態檔）已排除在版控之外，請勿上傳
- 抓取的是公開內容，但仍包含一般使用者的帳號與留言；請依所在單位的個資與研究倫理規範使用與保存
